众所周知github网页端只能删除文件，因此当你不小心创建不需要的文件夹时，想要删除就只能用git命令来解决了。

1、创建文件夹
     创建文件夹和创建文件一样，创建文件夹只是文件名后面多加个 “/” 即可，创建文件在名称后面加上后缀即可。
     这里需要注意的是，github不能有空文件夹，因此一定要有个文件。

2、删除文件夹
     方法1：删除文件夹意味着里面的文件你可能都不需要了，在github网页端提供了文件删除的按钮，
     因此当文件夹下的文件数量不多时，可以把文件都删除，此时可以变通的达到删除文件夹的目的（github不允许空文件夹存在）。
